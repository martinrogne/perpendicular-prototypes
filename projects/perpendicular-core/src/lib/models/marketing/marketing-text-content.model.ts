import { MarketingContent } from './marketing-content.model';

/**
 * This represents a piece of marketing text, as returned by the marketing engine. Note that
 * returned text types may be either plain, or in the format of HTML.
 */
export class MarketingTextContent extends MarketingContent {
}
