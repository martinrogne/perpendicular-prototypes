import { StoreLocation } from 'perpendicular-core';

/**
 * Provider specific version of the model
 */
export class WCSStoreLocation extends StoreLocation {
  /**
   * Default constructor
   */
  constructor() {
    super();
  }
}
