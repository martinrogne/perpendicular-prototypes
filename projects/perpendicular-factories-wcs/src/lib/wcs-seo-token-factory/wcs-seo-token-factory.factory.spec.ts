import { WCSSEOTokenFactory } from './wcs-seo-token-factory.factory';

describe('WCSSEOTokenFactory', () => {
  it('should create an instance', () => {
    expect(new WCSSEOTokenFactory()).toBeTruthy();
  });
});
