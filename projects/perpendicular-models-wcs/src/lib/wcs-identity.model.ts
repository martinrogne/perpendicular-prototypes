import { Identity } from 'perpendicular-core';

/**
 * Provider specific version of the model
 */
export class WCSIdentity extends Identity {
  /**
   * Default constructor
   */
  constructor() {
    super();
  }
}
