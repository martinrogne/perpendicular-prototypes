import { Organization } from 'perpendicular-core';

/**
 * Provider specific version of the model
 */
export class WCSOrganization extends Organization {
    /**
     * Default constructor
     */
    constructor() {
        super();
    }
}
