import { Address } from 'perpendicular-core';

/**
 * Provider specific version of the model
 */
export class WCSAddress extends Address {
  /**
   * Default constructor
   */
  constructor() {
    super();
  }
}
